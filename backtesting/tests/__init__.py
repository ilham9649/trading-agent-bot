"""Backtesting tests - verification and setup checks."""

from pathlib import Path
import sys

# Add project root to path for imports
sys.path.insert(0, str(Path(__file__).parent.parent.parent))

__all__ = []
